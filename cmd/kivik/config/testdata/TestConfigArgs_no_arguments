(*config.Config)({
  Contexts: (map[string]*config.Context) {
  },
  CurrentContext: (string) "",
  log: (*config.testLogger)({
    mu: (sync.Mutex) {
      state: (int32) 0,
      sema: (uint32) 0
    },
    logs: ([]string) {
    }
  }),
  finalizer: (func()) <nil>
})
